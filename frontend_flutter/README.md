# Never Alone - Flutter macOS App

**AI Companion for Elderly & Dementia Patients**

## 📱 Project Overview

This is the macOS desktop application for Never Alone, built with Flutter. It provides the user interface for elderly patients to have voice conversations with their AI companion.

## 🎯 Current Status: Task 5.1 - Project Setup ✅

- ✅ Flutter project created
- ✅ macOS platform configured
- ✅ Git configuration updated
- ⏳ Entitlements configuration (next step)
- ⏳ UI implementation (following)

## 🚀 Quick Start

```bash
# Get dependencies
flutter pub get

# Run on macOS
flutter run -d macos
```

## 📦 What's Tracked in Git

**✅ Included:**
- Source code (`lib/` folder)
- Platform config (`macos/Runner/*.entitlements`, `Info.plist`)
- Dependencies (`pubspec.yaml`)
- Documentation

**❌ Ignored (auto-generated):**
- `build/` - Build artifacts
- `.dart_tool/` - Dart tooling cache
- `.flutter-plugins*` - Plugin registrations
- `.pub-cache/`, `.pub/` - Package cache
- `macos/Flutter/ephemeral/` - Temporary Flutter files

The `.gitignore` is already configured by Flutter CLI.

## 📚 Full Documentation

- **Task Guide:** `/TASK_5.1_GUIDE.md` (step-by-step instructions)
- **Technical Docs:** `/docs/technical/`
- **UX Design:** `/docs/product/ux-design.md`

## 🔧 Next Steps

Follow `/TASK_5.1_GUIDE.md` for:
1. Configure macOS entitlements (microphone access)
2. Add dependencies to `pubspec.yaml`
3. Implement UI (screens + widgets)
4. Test basic functionality

---

*Created: November 11, 2025*
